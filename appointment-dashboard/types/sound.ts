export type SoundTypes = "message" | "notification" | "alert";

export const soundTypes: {
  message: SoundTypes;
  notification: SoundTypes;
  alert: SoundTypes;
} = {
  message: "message",
  notification: "notification",
  alert: "alert",
};
